name: Build targets

on:
  push:
    branches:
      - "**"

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        platform: ["kmp", "android", "ios", "jvm"]
    runs-on: ["self-hosted", "macOS"]
    timeout-minutes: 120
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}-${{ matrix.platform }}
      cancel-in-progress: true
    env:
      # Share Cargo's target directory between SDK modules in this job. Keep it outside Gradle's
      # build directory so clean does not delete it.
      CARGO_TARGET_DIR: ${{ github.workspace }}/cargo-target
      # Keep Cargo's registry and Git caches private to this matrix job. The self-hosted runner
      # can execute matrix jobs concurrently; sharing $HOME/.cargo makes them wait on Cargo's
      # package-cache lock.
      CARGO_HOME: ${{ github.workspace }}/.cargo
      # Use Git CLI so the credential.helper override below is honored. Cargo's default libgit2
      # transport does not apply this Git configuration and can wait for credentials.
      CARGO_NET_GIT_FETCH_WITH_CLI: "true"
      # Do not let a missing CI credential invoke a developer credential helper indefinitely.
      GIT_CONFIG_COUNT: "1"
      GIT_CONFIG_KEY_0: "credential.helper"
      GIT_CONFIG_VALUE_0: ""
      CARGO_INCREMENTAL: "0"
    steps:
      - uses: actions/checkout@v7.0.1

      - name: Determine Gradle tasks
        id: gradle_tasks
        run: |
          targets=()
          case "${{ matrix.platform }}" in
            kmp) targets+=("KotlinMultiplatform") ;;
            android) targets+=("Android") ;;
            ios) targets+=("IosArm64" "IosSimulatorArm64") ;;
            jvm) targets+=("Jvm") ;;
          esac

          tasks=""
          for target in "${targets[@]}"; do
            tasks="$tasks publish${target}PublicationToMavenLocal"
          done

          echo "tasks=$tasks" >> "$GITHUB_OUTPUT";

      - name: Set up JDK
        uses: actions/setup-java@v6.0.0
        with:
          distribution: "zulu"
          java-version: "17"

      - name: Setup rust Tool Chain
        uses: actions-rust-lang/setup-rust-toolchain@v1.17.0
        with:
          toolchain: "1.97.1"
          # Cache Cargo's registry/git data, but not target outputs. The latter are shared within
          # this job through cargo-target and are not portable between matrix jobs.
          cache-targets: false
          cache-key: rust-registry-v1

      - name: Install Android SDK components
        if: matrix.platform == 'android' || matrix.platform == 'kmp'
        run: |
          yes | sdkmanager --licenses
          yes | sdkmanager --install "ndk;28.2.13676358"

      - name: Ensure gradlew is executable
        run: chmod +x gradlew
      - name: Export iOS deployment target
        run: |
          echo "IPHONEOS_DEPLOYMENT_TARGET=$(grep '^IPHONEOS_DEPLOYMENT_TARGET=' gradle.properties | cut -d '=' -f2)" >> "$GITHUB_ENV"

      - name: Build
        env:
          CC_x86_64_unknown_linux_gnu: x86_64-linux-gnu-gcc
          CXX_x86_64_unknown_linux_gnu: x86_64-linux-gnu-g++
          AR_x86_64_unknown_linux_gnu: x86_64-linux-gnu-ar
          CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_LINKER: x86_64-linux-gnu-gcc
          CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_RUSTFLAGS: "-C link-args=-fstack-protector-all -lssp"
          CC_aarch64_unknown_linux_gnu: aarch64-linux-gnu-gcc
          CXX_aarch64_unknown_linux_gnu: aarch64-linux-gnu-g++
          AR_aarch64_unknown_linux_gnu: aarch64-linux-gnu-ar
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_RUSTFLAGS: "-C link-args=-fstack-protector-all -lssp"
          CARGO_TARGET_X86_64_PC_WINDOWS_GNU_LINKER: x86_64-w64-mingw32-gcc
        run: |
          set +e
          ./gradlew clean buildBindings ${{ steps.gradle_tasks.outputs.tasks }} -PreleaseBuild=false --console=plain &
          gradle_pid=$!

          trap 'kill "$gradle_pid" 2>/dev/null || true' INT TERM
          while kill -0 "$gradle_pid" 2>/dev/null; do
            sleep 60
            if kill -0 "$gradle_pid" 2>/dev/null; then
              elapsed=$(ps -p "$gradle_pid" -o etime= | xargs)
              echo "Rust/Gradle build still running (Gradle PID $gradle_pid, elapsed $elapsed)"
              pgrep -fl 'cargo|rustc|git' || true
            fi
          done

          wait "$gradle_pid"
          exit_code=$?
          trap - INT TERM
          exit "$exit_code"

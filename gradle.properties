#Gradle
org.gradle.jvmargs=-Xmx2048M -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options\="-Xmx2048M"
org.gradle.caching=true
org.gradle.configuration-cache=false
org.gradle.parallel=true

#Kotlin
kotlin.code.style=official
kotlin.mpp.enableCInteropCommonization=true

#Android
android.useAndroidX=true
android.nonTransitiveRClass=true
android.enableR8.fullMode=false

###########################
# Library wide POM metadata
###########################

# Artifact metadata
# Will only be used if no version is set via Gradle property (mostly from the GitHub release action)
ARTIFACT_VERSION=1.0.0

# POM metadata
POM_PACKAGING=aar
POM_INCEPTION_YEAR=2026

POM_URL=https://github.com/KapunSDK/kapun-sdk
POM_SCM_URL=https://github.com/KapunSDK/kapun-sdk
POM_SCM_CONNECTION=scm:git@github.com:KapunSDK/kapun-sdk.git
POM_SCM_DEV_CONNECTION=scm:git@github.com:KapunSDK/kapun-sdk.git

POM_LICENCE_NAME=License
POM_LICENCE_URL=https://github.com/KapunSDK/kapun-sdk/blob/main/LICENSE
POM_LICENCE_DIST=repo

# Developer information
POM_DEVELOPER_ID=ubique
POM_DEVELOPER_NAME=Ubique Innovation AG
POM_DEVELOPER_URL=https://www.ubique.ch/

# Artifact signing
# Read by the Gradle Maven Publish plugin, which signs all publications when enabled.
# Off by default so local publishing works without a signatory; the release workflow overrides
# it with -PRELEASE_SIGNING_ENABLED=true and supplies the key via ORG_GRADLE_PROJECT_signingInMemory*.
RELEASE_SIGNING_ENABLED=false

# Minimum iOS version for the Rust code.
# Exported into the environment by the build workflows, cargo/clang read it from there.
IPHONEOS_DEPLOYMENT_TARGET=16.4

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
